[pytest]
DJANGO_SETTINGS_MODULE = config.settings
markers =
  usecase(id): Canonical use-case id from docs/use-cases/CANONICAL.md
  quarantine: Legacy / anti-regression tests not part of the live product path
filterwarnings =
  ignore::DeprecationWarning
