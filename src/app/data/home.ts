/** Home hero copy — no product-surface card board on `/`. */
export const HOME_HERO = {
  preheader: 'DEML',
  heading: 'Control plane for ML data.',
  lede: 'Monitor sites, publish status, run analytics, and learn the stack — without clutter.',
} as const;
