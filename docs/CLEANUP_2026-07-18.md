# Final cleanup pass — 2026-07-18

Boundary enforced: DEML = user control plane + Angular + billing/consent/learning +
minimal FORJD BFF. FORJD = universal sealed data plane. No overlapping data planes.

## Removed / retired

| Item                                                           | Action                                     |
| -------------------------------------------------------------- | ------------------------------------------ |
| Firebase `ingestEvent` Redpanda / `DEML_INTERNODE_*` CI wiring | Stripped; deploy always `--clear-env-vars` |
| `infrastructure/dragonfly/Dockerfile` + `entrypoint.sh`        | Deleted                                    |
| `infrastructure/railway/dragonfly/railway.json`                | Deleted                                    |
| Marketing docs cards (Redpanda / ClickHouse / local workers)   | Rewritten to Fly/Vercel + FORJD            |
| Frontend `redpanda broker` label mapping                       | Removed                                    |
| README acknowledgements listing Redpanda as core stack         | Replaced with FORJD / Fly / Vercel         |

## Docs aligned

- `README.md` — hosts are Vercel + Fly + FORJD (not Railway Angular)
- `docs/VERCEL.md` — CORS/CSRF via `fly secrets` (not `railway variable set`)
- `infrastructure/dragonfly/RETIRED.md` + Railway frontend `RETIRED.md`
- Canonical cutover: `docs/CUTOVER.md`, `docs/PRODUCTION_CUTOVER_CHECKLIST.md`, `docs/FORJD_PLATFORM_HANDOFF.md`

## Intentionally kept

| Item                                          | Why                                              |
| --------------------------------------------- | ------------------------------------------------ |
| `backend/forjd/` BFF adapters                 | Minimal partner integration (rewrite + `fjsvc_`) |
| Railway `deml-backend` catalog entry          | Optional cold standby until DNS teardown         |
| `scripts/railway_retire_dataplane.py` / audit | Retirement tooling                               |
| BOOK/WHITEPAPER historical CONOPS             | Marked historical; not runtime                   |
| `functions` ingestEvent fail-closed stub      | Safe retired callable                            |

## Still optional (low priority)

- Drop unused `packages/deml-rate-limit` / Redis extras if no consumer remains
- Refresh generated marketing mirrors (`llms-full.txt`, search index) after next content sync
