# See ../AGENTS.md

This file is a pointer for compatibility with local tooling that looks inside the `.agents/` directory.

**The canonical and complete AGENTS.md is at the root of the repository:**

→ `../AGENTS.md`

Per the official spec (https://agents.md/), the root `AGENTS.md` is the primary location. All coding principles, architectural vision, and agent instructions live there.

For the project's custom agent skills, see the `skills/` subdirectory.
