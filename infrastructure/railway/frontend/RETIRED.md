# Retired — do not deploy

`deml-frontend` Railway hosting is retired.

- Primary Angular host is **Vercel** project `deml` (`docs/VERCEL.md`).
- Catalog entry lives under `services.json` → `retired`.
- Keep `railway.json` only as a cold-standby reference; do not attach a production domain.

Do not recreate active Railway frontend deploys for production traffic.
