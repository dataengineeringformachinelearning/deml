{
  "name": "viking-ui-docs",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Viking-UI documentation showcase (Astro) and static CSS build owner for the DEML design system.",
  "scripts": {
    "prebuild": "npm run build:static-css",
    "predev": "npm run build:static-css",
    "build:static-css": "node scripts/build-static-css.mjs",
    "dev": "astro dev --port 4300",
    "start": "astro dev --port 4300",
    "build": "astro build",
    "preview": "astro preview --port 4300"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "sass": "^1.89.2",
    "typescript": "~6.0.3"
  }
}
