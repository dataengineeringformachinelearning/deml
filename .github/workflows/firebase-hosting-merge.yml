# Retired: marketing is hosted on Vercel (project `marketing`, root `marketing/`).
# Keep this file as a no-op so historical links do not break; remove after DNS cutover is verified.

name: Deploy to Firebase Hosting on merge (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Firebase Hosting retired
        run: |
          echo "Marketing deploys via Vercel project marketing (marketing/vercel.json)."
          echo "Viking-UI Storybook deploys via Vercel project ui (packages/viking-ui/vercel.ui.json)."
