# Publish deml-rate-limit to PyPI
# Uses the repository-scoped PyPI API token until per-workflow Trusted Publishers are configured.
name: Publish deml-rate-limit to PyPI

on:
  push:
    branches:
      - main
    paths:
      - "packages/deml-rate-limit/**"
      - "packages/deml-rate-limit/pyproject.toml"
      - ".github/workflows/publish-deml-rate-limit.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: publish-deml-rate-limit
  cancel-in-progress: false

jobs:
  publish:
    name: Build and publish deml-rate-limit to PyPI
    runs-on: ubuntu-latest
    environment: pypi-rate-limit
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build twine

      - name: Build package
        run: |
          cd "packages/deml-rate-limit"
          python -m build

      - name: Validate package metadata
        run: twine check packages/deml-rate-limit/dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.PYPI_TOKEN }}
          packages-dir: "packages/deml-rate-limit/dist"
          skip-existing: true
