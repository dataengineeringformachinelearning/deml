# Retired: marketing previews are Vercel preview deployments for project `marketing`.

name: Deploy to Firebase Hosting on PR (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Firebase Hosting PR previews retired
        run: echo "Use Vercel preview deployments for marketing/ instead."
