# Retired: ui.dataengineeringformachinelearning.com is Vercel project `ui`
# (packages/viking-ui/vercel.ui.json → Storybook static root).

name: Deploy Viking-UI Docs (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Firebase Hosting for Viking-UI docs retired
        run: |
          echo "Deploy Storybook via Vercel project ui (root packages/viking-ui)."
          echo "Config: packages/viking-ui/vercel.ui.json"
