# Retired: public Storybook hosting (ui.deml.app / deml-ui) is removed.
# Viking-UI components remain in packages/viking-ui; use local Storybook or Chromatic.

name: Deploy Viking-UI Docs (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Public Storybook hosting retired
        run: |
          echo "ui.deml.app / deml-ui Vercel project removed."
          echo "Keep components in packages/viking-ui; run Storybook locally or Chromatic."
