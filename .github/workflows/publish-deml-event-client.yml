# Publish deml-event-client to PyPI
# Uses the repository-scoped PyPI API token until per-workflow Trusted Publishers are configured.
name: Publish deml-event-client to PyPI

on:
  push:
    branches:
      - main
    paths:
      - "packages/deml-event-client/**"
      - "packages/deml-event-client/pyproject.toml"
      - ".github/workflows/publish-deml-event-client.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: publish-deml-event-client
  cancel-in-progress: false

jobs:
  publish:
    name: Build and publish deml-event-client to PyPI
    runs-on: ubuntu-latest
    environment: pypi-event-client
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build twine

      - name: Build package
        run: |
          cd "packages/deml-event-client"
          python -m build

      - name: Validate package metadata
        run: twine check packages/deml-event-client/dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.PYPI_TOKEN }}
          packages-dir: "packages/deml-event-client/dist"
          skip-existing: true
