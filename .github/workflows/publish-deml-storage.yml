# Publish deml-storage to PyPI
# Uses the repository-scoped PyPI API token until per-workflow Trusted Publishers are configured.
name: Publish deml-storage to PyPI

on:
  push:
    branches:
      - main
    paths:
      - "packages/deml-storage/**"
      - "packages/deml-storage/pyproject.toml"
      - ".github/workflows/publish-deml-storage.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: publish-deml-storage
  cancel-in-progress: false

jobs:
  publish:
    name: Build and publish deml-storage to PyPI
    runs-on: ubuntu-latest
    environment: pypi
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build twine

      - name: Build package
        run: |
          cd "packages/deml-storage"
          python -m build

      - name: Validate package metadata
        run: twine check packages/deml-storage/dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.PYPI_TOKEN }}
          packages-dir: "packages/deml-storage/dist"
          skip-existing: true
